# iOS FullUI 黑色水滴刷新反馈

状态：2026-09-10 用户确认沿用此前交互预览的造型，调整为跟手分裂、悬停等待松手、圆形直接返回。原生视觉仍待验收。

首页和收藏共用 `LiquidRefreshIndicator`。按住时仅随拉距改变形状；手指停止移动，水滴就保持当前形状，没有文字提示。拉到视觉阈值后即断开为悬停圆圈，必须松手才开始请求和加载旋转。

## 触发与生命周期

首页仍由 `refreshable` 判定刷新条件，收藏使用 `UIRefreshControl`。`PullRefreshReleaseGate` 在回调之后、请求及加载状态之前等待所属 scroll view 结束 tracking/dragging/pan。它不安装额外手势；只在等待松手时以可取消的异步任务观察原生状态。手势取消、视图离开窗口或任务取消时退出。收藏等待及请求共用一个任务避免重复触发，wrapper 拆卸时取消。

首页的 gate 绑定现有滚动探针所属 scroll view。首页 `refreshAll` 和收藏 `onRefreshChange(true)` 均在松手后才执行，之后递增 `refreshCycle`；即使极短请求的布尔开始／结束被 SwiftUI 合并，也不会漏掉动画。不修改共享网络或同步规则。

`LiquidRefreshPullState` 在拉距达到 108pt 后锁定悬停位置，继续拉远或小幅回拉不移动圆圈；拉距回到静止位置才解除等待。它也防止一次已刷新的下拉在回收后重新画出静止水滴。只有动画结束且拉距回到静止位置（不超过 1pt）才重新响应下拉；回收结束时读取最新拉距，不使用异步任务休眠前捕获的旧值。

## 形状与动画

- 恢复已选定预览的 26pt 圆滴、连接曲线与横向拉伸比例。
- 下拉进度前 75% 伸长，后 25% 直接随手指收颈并分离，没有额外时间动画追赶手势。
- 达到阈值时圆圈悬停，白色细弧静止；停住手指不会自动分裂或发起刷新。
- 松手刷新继承悬停位置，不重新播放分裂。原生刷新阈值与视觉阈值不同的情况，仅补完剩余位移。
- 请求完成后，圆圈用 0.40 秒上移淡出，大小保持不变，整个返程 separation 始终为 1，不重新连接、不拉丝、不缩成水滴。极短请求保留最少 0.20 秒加载展示，不延迟数据更新。
- 加载使用白色细弧。Canvas 仅绘制 120pt 局部区域，连接和圆滴分别填充，避免重叠路径镂空。
- 减弱动态效果使用固定圆滴与静态加载弧；后台暂停旋转，不拦截触摸，VoiceOver 提供刷新状态。

## 窗口适配

使用组件所属 UIView 的 window、safeAreaInsets 和坐标转换定位，不查询全局 key window。公开 UIKit 没有传感器缺口精确轮廓 API，安全区是保守推断，不是硬件边缘测量。

手机竖屏、组件铺满窗口且顶部安全区至少 44pt 时，在安全区上方 20pt 起笔，与常见灵动岛／刘海内重叠接合。无缺口屏、横屏、iPad 和窄栏使用独立水滴。布局、窗口和安全区改变后重新测量。所有改动只在 FullUI。

## 本版验证（2026-09-10）

实际动画、悬停状态与 release gate 源码的 Swift 6 回归共 77 项断言通过：按住时形状不随时间变化、跟手收颈、阈值锁定、松手无位置跳变、返程全程断开且保持圆形尺寸、上移淡出、再次下拉、按住不请求及松手只请求一次。不是 Package 测试，不替代设备视觉检查。

本版 MCP BuildProject 成功（14.02 秒），Navigator error 为 0。原生组件固定状态快照中，返程为独立圆形、没有连接颈；临时 hosting 窗口导致悬停态底部裁切，不能据此判断完整页面外形。

最后源码修改及最后 snippet 后，iPhone 17 Pro / iOS 27 新 InstallAndRun 成功。仅做空 capture 时发现页面有外部操作，因此停止自动触摸以避免干扰。本轮未完成自动手势验收；实际保持、松手请求、完整回落及二次下拉过程未作通过结论。收藏、刘海屏、iPad 和其他宿主未实测。旧版截图与构建不作为本版通过证据。
